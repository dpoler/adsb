// Temporary placeholders for subsystems that haven't been ported to Linux
// yet. metar and atis are ported (platform_linux/metar_linux.cpp,
// atis_linux.cpp). Kept as an empty TU so CMake's historical
// app_stubs.cpp entry doesn't break.

// (intentionally empty)
